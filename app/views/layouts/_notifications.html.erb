<div class="notifications">
	<% view_notifications.each do |notification| %>
		<div class="notification-<%=notification.id%>">
			<%= notification.id %>
			<%= link_to 'Visto', notification_check_path(notifications: [notification.id]), :method => :put, :remote => true%>
		</div>
	<%end%>
</div>